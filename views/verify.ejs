<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify OTP</title>

  <style>
    body {
      min-height: 100vh;
      background-image: url('https://images.unsplash.com/photo-1588345921523-c2dcdb7f1dcd?q=80&w=1170&auto=format&fit=crop');
      background-size: cover;
      background-position: center;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .otp-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 16px;
      padding: 30px 25px;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.3);
      text-align: center;
      animation: fadeUp 0.6s ease;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(25px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .otp-card h3 {
      font-weight: 700;
      color: #333;
    }

    .otp-card p {
      font-size: 0.95rem;
      color: #555;
      margin-bottom: 20px;
    }

    .otp-input {
      text-align: center;
      font-size: 1.3rem;
      letter-spacing: 5px;
      font-weight: 600;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid #ccc;
    }

    .otp-input:focus {
      border-color: #28a745;
      box-shadow: 0 0 0 0.2rem rgba(40,167,69,0.25);
    }

    .btn-verify {
      border-radius: 30px;
      padding: 10px;
      font-weight: 600;
      font-size: 1rem;
      width: 100%;
    }

    .helper-text {
      font-size: 0.85rem;
      color: #777;
      margin-top: 15px;
    }

    @media (max-width: 576px) {
      .otp-card {
        margin: 20px;
        max-width: 100%;
      }
    }
  </style>
</head>

<body>

  <div class="otp-card">
    <form action="/verify-otp" method="POST">

      <h3>OTP Verification</h3>
      <p>A 6-digit OTP has been sent to your Gmail</p>
      <hr>

      <input
        class="form-control otp-input mb-3"
        type="text"
        name="otp"
        placeholder="Enter OTP"
        maxlength="6"
        required
      >

      <button type="submit" class="btn btn-success btn-verify">
        Verify OTP
      </button>

      <p class="helper-text">
        Please check your inbox or spam folder
      </p>

    </form>
  </div>

</body>
</html>
