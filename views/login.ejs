<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>

  <style>
    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .login-card {
      background: #ffffff;
      border-radius: 16px;
      padding: 30px 25px;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.25);
      animation: fadeUp 0.6s ease;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .login-card h3 {
      font-weight: 700;
      color: #333;
    }

    .login-card hr {
      margin: 15px 0 25px;
    }

    .login-form-control {
      border-radius: 10px;
      padding: 12px 14px;
      font-size: 15px;
      border: 1px solid #ccc;
    }

    .login-form-control:focus {
      border-color: #1a73e8;
      box-shadow: 0 0 0 0.15rem rgba(26,115,232,0.25);
    }

    .forgot-link {
      display: block;
      text-align: right;
      font-size: 0.9rem;
      text-decoration: none;
      color: #1a73e8;
      margin-bottom: 20px;
    }

    .forgot-link:hover {
      text-decoration: underline;
    }

    .btn-login {
      background: #1a73e8;
      color: #fff;
      font-weight: 600;
      border-radius: 30px;
      padding: 10px 26px;
    }

    .btn-login:hover {
      background: #1666cc;
    }

    .btn-signup {
      border-radius: 30px;
      padding: 10px 26px;
      font-weight: 600;
    }

    .helper-text {
      font-size: 0.9rem;
      color: #777;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>

  <div class="login-card">
    <form id="main-div"
          action="/buyandsell/login"
          method="POST"
          class="text-center">

      <h3>Welcome Back</h3>
      <p class="helper-text">Login to continue buying & selling</p>
      <hr>

      <input class="form-control login-form-control mb-3"
             type="email"
             placeholder="Email"
             name="femail"
             required>

      <input class="form-control login-form-control mb-2"
             type="password"
             placeholder="Password"
             name="fpassword"
             required>

      <a href="/buyandsell/password_recovery" class="forgot-link">
        Forgot Password?
      </a>

      <div class="d-flex justify-content-center gap-2">
        <button type="submit" class="btn btn-login">
          Login
        </button>
        <a href="/buyandsell/signup_gmail" class="btn btn-outline-success btn-signup">
          Sign Up
        </a>
      </div>

    </form>
  </div>

</body>
</html>
