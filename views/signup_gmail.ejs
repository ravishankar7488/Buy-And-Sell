<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Send OTP</title>

  <style>
    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .otp-card {
      background: #ffffff;
      border-radius: 16px;
      padding: 30px 25px;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.25);
      animation: fadeUp 0.6s ease;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .otp-card h3 {
      font-weight: 700;
      color: #333;
    }

    .otp-card p {
      font-size: 0.95rem;
      color: #777;
    }

    .otp-card hr {
      margin: 15px 0 25px;
    }

    .login-form-control {
      border-radius: 10px;
      padding: 12px 14px;
      font-size: 15px;
      border: 1px solid #ccc;
    }

    .login-form-control:focus {
      border-color: #1a73e8;
      box-shadow: 0 0 0 0.15rem rgba(26,115,232,0.25);
    }

    .btn-send {
      background: #17a2b8;
      color: #fff;
      font-weight: 600;
      border-radius: 30px;
      padding: 10px 30px;
    }

    .btn-send:hover {
      background: #138496;
    }

    .hint-text {
      font-size: 0.85rem;
      color: #666;
      margin-top: 15px;
    }
  </style>
</head>

<body>

  <div class="otp-card text-center">
    <form action="/send-otp" method="POST">

      <h3>Email Verification</h3>
      <p>We’ll send a one-time password to your Gmail</p>
      <hr>

      <input class="form-control login-form-control mb-3"
             type="email"
             name="email"
             placeholder="Enter your Gmail"
             required>

      <button type="submit" class="btn btn-send">
        Send OTP
      </button>

      <p class="hint-text">
        Please check your inbox and spam folder
      </p>

    </form>
  </div>

</body>
</html>
